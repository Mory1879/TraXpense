{
    "name": "mobile",
    "private": true,
    "scripts": {
      "mock-firebase:run": "firebase emulators:start --import=.",
      "mock-firebase:run:with-export": "firebase emulators:start --export-on-exit --import=.",
      "mock-firebase:save": "firebase emulators:export ."
    }
}